<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.algorigo.algorigoblelibrary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SampleBleDeviceActivity" />
        <activity android:name=".smart_chair.SC01DeviceActivity" />
        <activity android:name=".smart_chair.SC20DeviceActivity" />
        <activity android:name=".smart_chair.SC21DeviceActivity" />
        <activity android:name=".smart_chair.SC30DeviceActivity" />
        <activity android:name=".bruxweeper.BruxweeperActivity" />
        <activity android:name=".mldp_terminal.MLDPTerminalActivity" />
        <activity android:name=".carseat.CarSeatDeviceActivity" />
    </application>

</manifest>